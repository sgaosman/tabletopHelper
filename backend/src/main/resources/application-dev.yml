spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/questkeeper
    username: questkeeper
    password: questkeeper
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.questkeeper: DEBUG
    org.hibernate.SQL: DEBUG
